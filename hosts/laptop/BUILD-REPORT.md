# `laptop` — build report

Nothing built yet. See `BOOTSTRAP.md` for the procedure.

Start this file with the hardware audit from BOOTSTRAP step 0, verbatim output included,
before running the first step. `hosts/server/BUILD-REPORT.md` is the format: dated
sections, appended in order, recording what was built, what broke, what was measured, and
what was decided and why. It is the only record of the decisions that are not visible in
the code, and it earned its length.
