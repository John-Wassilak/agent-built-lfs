# /lfs-audit -- server, 2026-09-22 (native, LFS/BLFS 13.1)

Run after the 13.1 version sweep completed (0 packages behind the books).
Prior report: audit-2026-09-21.md

## Raw findings

A. disk      : / 76G free of 130G (39% used). /mnt/big_drive 116G free of 446G.
               journal 32M. /boot holds exactly one kernel (7.1.8-lfs-13.1-systemd),
               which is the one grub.cfg boots. 9 coredumps ~300M, all mpv.
               ~/.npm 2.6G, ~/.cache/pip 108M. /tmp 519M (508M is this session's
               scratch). 173 scratch files older than 7d.
B. services  : 0 failed units. All 5 project units enabled AND active
               (lfsmaint-check.timer, cpufreq-governor, sshd, iptables, update-pki.timer).
C. processes : no zombies. 3 procs on a (deleted) claude.exe -- npm self-update of the
               agent tooling, not system packages. Listening: sshd 22, resolved 53/5355,
               tailscaled. No duplicate singletons.
D. security  : 21 SUID/SGID, all owned by a package, same count as 2026-09-21. Xorg is
               in the set (setuid restored today after the 13.1 rebuild dropped it).
               No world-writable dirs without sticky. No non-root UID 0. No empty
               password fields. World-writable files are uv/venv .lock files under
               /home/john only -- unchanged from the prior audit.
               Advisories: 33 total, 8 affecting installed packages, 5 Critical/High
               (libaom, libxml2, pcre2, expat, firefox; curl/xz/openssl Medium).
E. hardware  : every PCI device has a bound driver except Broadcom BCM4321 (14e4:4329),
               which is the deliberate, already-recorded non-binding in kernel-config.sh.
F. features  : governor schedutil. microcode 0x2f, no old_microcode in bugs. vmscape
               listed with no mitigation symbol available (recorded 09-21, KVM not built).
               GPU bound to nvidia, not llvmpipe. VA-API now live (NVDEC direct backend,
               H264 High). ffmpeg hwaccels: vdpau cuda vaapi drm vulkan. mpv hwdec=vdpau-copy.
G. hygiene   : 1279 strip candidates (readelf .debug_info). 13026 uncompressed man pages
               against 7 compressed. 130 uncompressed info pages. No /usr/lib/debug tree.
H. manifests : 333 packages, 108750 files. verify: nothing unexplained.
I. boot      : 15.066s (3.355 kernel + 11.710 userspace). Slowest:
               systemd-networkd-wait-online 5.441s, dev-sdb2.device 4.600s,
               fsck on the data disk 4.556s. 41 enabled unit files.
J. mounts    : findmnt --verify: "Success, no errors or warnings detected".
               fstab LABEL=LFSROOT/LFSSWAP match lsblk exactly; big_drive by UUID present.

## Critical

- mpv is still linked against ffmpeg 8. The 9.0.1 bump changed the soname
  (libavcodec.so.62 -> .so.63), so /usr/lib/libavcodec.so.62 still resolves to the
  orphaned ffmpeg-8 file and mpv + libmpv are its only consumers. mpv also SIGSEGV'd
  9 times (6 at session start 09-21 20:18, 3 when X was stopped 09-22 13:03).
  Fix: rebuild mpv (seq 179) against the current ffmpeg.

## Distinguished, NOT a problem

- The other soname-bumped libraries kept their soname, so their symlinks point at the
  NEW file and every consumer resolves correctly: libarchive.so.13 -> 13.8.9,
  libevent-2.1.so.7 -> 7.0.2, libpci.so.3 -> 3.15.0, libpipewire-0.3.so.0 -> 0.1608.0,
  libwireplumber-0.5.so.0 -> 0.515.0. Their predecessor FILES are dead weight only.

## Since the 2026-09-21 audit

Closed: lgi/awesome crash, grub.cfg fs-uuid, lfsmaint BOOK_RELEASE, advisories
PermissionError, verify's lexists bug, fstab ordering, awesome SYSCONFDIR,
host.toml kernel. Manifests now describe the running tree (the state move ran).
New since: the ffmpeg-8/mpv soname gap, and Xorg's setuid loss (found and fixed today).
Unchanged: BCM4321 unbound (deliberate), vmscape (no mitigation exists),
strip/compress still owed from the 09-21 panic.

## Remediation done the same day

- mpv rebuilt against ffmpeg 9 (libavcodec.so.63); zero consumers of the ffmpeg-8
  sonames left. Verified decoding via --hwdec=vdpau-copy, not just linking.
- 326 MB of coredumps cleared (all nine were that broken mpv).
- Firewall: live script was the 13.0 text and the box read
  net.ipv4.conf.all.accept_redirects = 1. Installed the 13.1 recipe's version
  (one-line diff, SSH rule byte-identical); now 0. The operator's own hand-run
  ~/Config/common/firewall/firewall.sh had the same gap and was patched and
  pushed there too (acbc493).
- Strip: 1280 inodes, 0 skipped, 0 failures, 175 MB of file size. Candidates now 0.
  Hardlink sets intact (git 150, gcc 3, perl 2), no zero-length objects, ldd clean.
- Compression: man 123M -> 77M, info 37M -> 9.4M on disk. 511 symlinks retargeted
  to their .gz targets; 647 hardlinked man pages left uncompressed because gzip
  refuses them rather than break the links. lfsmaint verify now reports 12508
  "compressed after install" and still nothing unexplained.
- connmark kernel gap recorded in kernel-config.sh, not enabled.

Still open: 8 advisories (5 High). The strip saving mostly does not show in df yet --
install unlinks the old inode while running processes still map it.
